name: 01-Building-blocks

on: 
  workflow_dispatch:

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "Hello world"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Succesfull step
              run: |
                echo "I will succeed"
                exit 0
            - name: Say goodbye
              run: echo "Goodbye!"